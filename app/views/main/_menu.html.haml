#menu.column.span-24.last
  .column.tabs.span-10
    %ul
      %li.label Tasks:
      %li= link_to "My", my_projects_path#, :class=> @tabs_state[:index] ? "selected" : ""
      %li= link_to "Foreign", foreign_projects_path#, :class=> @tabs_state[:foreign] ? "selected" : ""
  #userData.column.prepend-9.span-4
    %span#userLogin
      = current_user.email
      (#{link_to 'Sign out', destroy_user_session_path, :method => :delete})
#completeness.column.prepend-17.span-7.last.tabs{:style => "padding-left: 680px;"}
  %ul
    %li
      %a{:href => incompleted_tasks_path, :class=> "inverseTab"}Incompleted
      =#, :class=>@tabs_state[:incompleted] ? "selected inverseTab" : "inverseTab" 
        
    %li
      %a{:href=>completed_tasks_path, :class=> "inverseTab"} Completed
      =#, :class=> @tabs_state[:completed] ? "selected inverseTab" : "inverseTab"

